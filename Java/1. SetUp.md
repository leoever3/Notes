### Java

#### JDK

Java Standard Edition Development Kit

```bash
$ java --version
openjdk 17.0.1 2021-10-19
OpenJDK Runtime Environment Homebrew (build 17.0.1+0)
OpenJDK 64-Bit Server VM Homebrew (build 17.0.1+0, mixed mode, sharing)
$ javac --version
javac 17.0.1
```

#### IDE

Integrated Development Environment

###### IntelliJ

###### Eclipse

#### MySQL

Set up MySQL

```bash
$touch .bash_profile

$ls -a

$open -t .bash_profile

//in the text
copy the path of the mysql_bin
expor PATH=${PATH}: /usr/local/mysql/bin
//save the text

$mysql -u root -p
```

```bash
$ mysql --version
mysql  Ver 8.0.27 for macos11 on arm64 (MySQL Community Server - GPL)
```

### Extra

CS61B

#### Homebrew

```bash
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
```

```bash
$ brew --version
Homebrew 3.3.5
Homebrew/homebrew-core (git revision 7d8991e9851; last commit 2021-11-22)
Homebrew/homebrew-cask (git revision e275f90d0d; last commit 2021-11-23)
```

#### Git

```bash
 brew install --cask java
 brew install git
```

```bash
$ git --version
git version 2.30.1 (Apple Git-130)
```

#### Sublime

Text editor

### Issues

1.**Homebrew** is installed in a different place.

According to https://docs.brew.sh/Installation, `/usr/local` is for Intel binaries, `/opt/homebrew` for ARM

2.Many **path** problems

To be continued!